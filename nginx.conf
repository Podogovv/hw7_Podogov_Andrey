events {}

http {
    upstream ml_backend {
        server host.docker.internal:5001;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://ml_backend;
        }

        location /health {
            proxy_pass http://ml_backend/health;
        }
    }
}
